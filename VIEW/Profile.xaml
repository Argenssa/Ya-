<UserControl x:Class="Ya_.VIEW.Profile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ya_.VIEW"
             mc:Ignorable="d" 
            d:DesignHeight="700" d:DesignWidth="932">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1"  MaxHeight="735" VerticalScrollBarVisibility="Auto">
            <WrapPanel>
            <WrapPanel Name="Playlistss">

            </WrapPanel>
                
                    <StackPanel Height="300">
                        <Label FontSize="80" Margin="20 0 0 0" HorizontalAlignment="Center" Foreground="White" MouseLeftButtonDown="OpemModal">+</Label>


                </StackPanel>
               
            </WrapPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
